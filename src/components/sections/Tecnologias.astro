---
import PERSONA from "@data/persona";
import Icons from "../Icons.astro";

const { tecnologias } = PERSONA;
---

<p
  class="uppercase text-center block mb-5 font-extrabold text-gray-600 dark:text-slate-200"
>
  Tecnologías
</p>
<div class="flex justify-center md:justify-evenly align-middle flex-wrap">
  {
    tecnologias.map((tecnologia) => (
      <span class="text-center m-2 dark:text-slate-300 text-slate-600">
        <Icons name={tecnologia} />
        <small class="text-center">{tecnologia}</small>
      </span>
    ))
  }
</div>
